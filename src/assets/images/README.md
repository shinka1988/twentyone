# 画像フォルダ

写真は用途ごとに分けて保存します。

- `hero/` メインビジュアル
- `salon/` 店内・ABOUT
- `menu/` メニュー写真
- `gallery/` TOPギャラリー
- `staff/` スタッフ写真

現在のサイトは `/public/images/...` を参照する構成です。
画像を実際に表示する場合は、プロジェクトの `public/images/` に同じフォルダ構成で配置してください。
