---
import { heroImage } from "../data/salon";
---

<section
  id="top"
  class="hero hero-image"
  style={heroImage ? `--hero-image: url(${heroImage})` : undefined}
  aria-label="Twenty one"
>
  <span class="sr-only">Twenty one</span>
</section>
